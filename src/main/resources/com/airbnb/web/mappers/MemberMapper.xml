<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.airbnb.web.mappers.MemberMapper">

<!-- 	<select id="list" resultType="com.airbnb.web.domains.MemberDTO"
	 parameterType="com.airbnb.web.domains.Command" >	
	select t2.* from(
		   select rownum seq,t.* from (
		     select 
		    m.mem_id id, 
		    m.name name,
		    m.gender gender,
		    m.reg_date regDate,
		    m.email email,
		    m.profile_img profileImg,
		    m.role role,
		    m.phone phone,
		    m.major_seq majorSeq    
		   from Member m where role='STUDENT' order by reg_date desc) t)t2
		   where t2.seq between #{start} and #{end}
		   order by t2.seq asc

	</select> 
	

	<select id="findByName" parameterType="string" resultType="com.airbnb.web.domains.MemberDTO">
	select * from member where name = #{name}
		</select> -->
	<select id="count" resultType="com.airbnb.web.domains.Retval">
	select count(*) as count from Member where role='STUDENT'
		</select>
	
	<select id="existId" parameterType="com.airbnb.web.domains.MemberDTO" resultType="int">
		select count(*) as count from member where mem_id = #{id}
	</select>
	
	
	
	
	<delete id="delete" parameterType="com.airbnb.web.domains.MemberDTO"></delete>
  
  
</mapper>